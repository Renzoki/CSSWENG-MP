<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Complete Editor with Right-Side Drag</title>
    <link rel="stylesheet" href="create-page.css">
    <script src="https://cdn.jsdelivr.net/npm/sortablejs@1.15.0/Sortable.min.js"></script>
</head>
<body>
    <div class="back-button-container">
        <a href="#" onclick="history.back()" class="back-button">
            <img src="back-icon.png" alt="Back" class="back-icon">
        </a>
    </div>

    <div class="editor-wrapper">
        <div class="editor-interface">
            <!-- Toolbar -->
            <div class="toolbar">
                <!-- Text Section -->
                <div class="toolbar-section">
                    <div class="undo-redo-buttons">
                        <button class="format-btn" title="Undo" onclick="document.execCommand('undo', false, null);">
                            <img src="undo-icon.png" alt="Undo" class="toolbar-icon">
                        </button>
                        <button class="format-btn" title="Redo" onclick="document.execCommand('redo', false, null);">
                            <img src="redo-icon.png" alt="Redo" class="toolbar-icon">
                        </button>
                    </div>
                    <div class="section-label">Text</div>
                    
                    <div class="format-buttons">
                        <button class="format-btn" title="Bold" onclick="document.execCommand('bold', false, null);">
                            <img src="bold.png" alt="Bold" class="toolbar-icon">
                        </button>
                        <button class="format-btn" title="Italic" onclick="document.execCommand('italic', false, null);">
                            <img src="italic.png" alt="Italic" class="toolbar-icon">
                        </button>
                        <button class="format-btn" title="Underline" onclick="document.execCommand('underline', false, null);">
                            <img src="underline.png" alt="Underline" class="toolbar-icon">
                        </button>
                    </div>
                </div>
                
                <!-- Alignment Section -->
                <div class="toolbar-section">
                    <div class="section-label">Alignment</div>
                    <div class="format-buttons">
                        <button class="format-btn" title="Center" onclick="setTextAlign('center')">
                            <img src="center-middle.png" alt="Center" class="toolbar-icon">
                        </button>
                    </div>
                </div>
                
                <!-- Insert Section -->
                <div class="toolbar-section">
                    <div class="section-label">Insert</div>
                    <div class="format-buttons">
                        <button class="format-btn" title="Insert Image" onclick="insertImage()">
                            <img src="image.png" alt="Image" class="toolbar-icon">
                        </button>
                        <button class="format-btn" title="Insert Link" onclick="insertLink()">
                            <img src="link.png" alt="Link" class="toolbar-icon">
                        </button>
                    </div>
                </div>
                
                <!-- List Section -->
                <div class="toolbar-section">
                    <div class="section-label">List</div>
                    <div class="format-buttons">
                        <button class="format-btn" title="Bullet List" onclick="document.execCommand('insertUnorderedList', false, null);">
                            <img src="list-dotted.png" alt="Bullets" class="toolbar-icon">
                        </button>
                        <button class="format-btn" title="Numbered List" onclick="document.execCommand('insertOrderedList', false, null);">
                            <img src="list-numbered.png" alt="Numbers" class="toolbar-icon">
                        </button>
                        <button class="format-btn" title="Checklist" onclick="insertChecklist()">
                            <img src="list-mixed.png" alt="Checklist" class="toolbar-icon">
                        </button>
                    </div>
                </div>
            </div>
            
            <!-- Pages Container -->
            <div class="pages-container" id="pagesContainer">
                <!-- Permanent first page with title and author -->
                <div class="page-with-handle permanent-page">
                    <div class="page-editor">
                        <div class="title-author-container">
                            <div class="title-field">
                                <div class="editable-title" contenteditable="true" data-placeholder="Title"></div>
                            </div>
                            <div class="author-field">
                                <div class="editable-author" contenteditable="true" data-placeholder="Author"></div>
                            </div>
                        </div>
                        <div class="page-content" contenteditable="true">
                            Start editing here...
                        </div>
                    </div>
                    <div class="drag-handle hidden-handle">≡</div>
                </div>
                <!-- Add button after first page -->
                <div class="add-page-btn-container">
                    <button class="add-page-btn" onclick="addNewPage(this)">+</button>
                </div>
            </div>
        </div>
        
        <!-- Action Buttons -->
        <div class="action-buttons">
            <button class="action-button draft">Draft</button>
            <button class="action-button post">Post</button>
        </div>
    </div>

    <script>
        let pageCount = 1;
        
        function addNewPage(clickedButton) {
            pageCount++;
            const pagesContainer = document.getElementById('pagesContainer');
            const newPage = document.createElement('div');
            newPage.className = 'page-with-handle';
            newPage.innerHTML = `
                <div class="page-editor">
                    <div class="page-content" contenteditable="true">
                        Start editing your new page here...
                    </div>
                </div>
                <div class="drag-handle">☰</div>
            `;
            
            // Create new add button
            const newAddButton = document.createElement('div');
            newAddButton.className = 'add-page-btn-container';
            newAddButton.innerHTML = `
                <button class="add-page-btn" onclick="addNewPage(this)">+</button>
            `;
            
            // Insert both new elements after the clicked button's container
            const buttonContainer = clickedButton.parentNode;
            pagesContainer.insertBefore(newPage, buttonContainer.nextSibling);
            pagesContainer.insertBefore(newAddButton, buttonContainer.nextSibling.nextSibling);
        }

        // Initialize drag-and-drop
        document.addEventListener('DOMContentLoaded', function() {
            new Sortable(document.getElementById('pagesContainer'), {
                handle: '.drag-handle',
                animation: 150,
                ghostClass: 'drag-ghost',
                onStart: function() {
                    document.querySelectorAll('.drag-handle').forEach(h => {
                        h.style.color = '#555';
                    });
                },
                onEnd: function() {
                    document.querySelectorAll('.drag-handle').forEach(h => {
                        h.style.color = '#999';
                    });
                }
            });
        });
    </script>
</body>
</html>